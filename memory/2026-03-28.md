
## Paper Trader League / Tuque — Mar 28, 2026
- Diagnosed bot/runtime state across seasons after JL asked to get seasons 2 and 3 trading again and review season 4 strategy.
- Found Season 4 was active and trading, but the live executor path is long-only; short/fade logic should only be added if runtime support is confirmed.
- Found Season 3 had previously broken pump token refresh / zero-portfolio emergency-exit behavior; later observed it recovered after patch/restart with `pump_surfer` trading again and pump token refresh showing 10 tokens.
- Found Season 2 was not merely idle: the `data_ingest` service had been miswired to `DEFAULT_SEASON_ID=season-004`, so the former S2 lane was effectively hijacked and no fresh S2 metrics/orders were being written.
- Corrective action taken: updated `paper-trader-league/docker-compose.yml` so `data_ingest` explicitly sets `DEFAULT_SEASON_ID=season-002`, `AUTO_BOOTSTRAP_SEASON=true`, and `DEFAULT_STARTING_BTC=0.05`, then restarted `data_ingest`.
- At flush time, the config/env fix was verified in the running container, but fresh Season 2 DB rows had not yet been observed; next check should confirm new `bot_metrics` or `bot_orders` for `season-002` before claiming recovery.
- Repo note: recent commit present in paper-trader-league was `c8df64b Fix season runtime dust handling and season wiring`.

## Rotation checkpoint (2026-03-28 16:57 UTC)
- Rotation recommended by chat pace policy.

## Paper Trader League / Season 4 backup executor — Mar 28, 2026
- Implemented `services/bot_executor/src/main_backup.py` as a simpler season-004 fallback executor.
- Backup path mirrors live marks from the freshest other season with `BTCUSDT` into `season-004`, then trades the two active bots long-only from mark-history signals instead of relying on `coinbase_big_gainers` / `coinbase_big_losers` tables.
- Added a compose switch so `BOT_EXECUTOR_ENTRYPOINT=src/main_backup.py` will run the backup executor in place of the default executor.
- Local validation: `trade_engine` health OK, backup script compiled, current live source auto-resolved to `season-003`, and candidate ranking produced one live setup for each season-004 bot after loosening the loser-bot rebound thresholds.
- One dry-run cycle mirrored fresh marks into `season-004` successfully and reached funding decisions for both bots; actual buys require running the loop live so the BTC->USDT funding fills settle first.
