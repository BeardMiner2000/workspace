# Session Log — March 26, 2026

## Summary
Formalized and extended a chat-memory system so conversation context survives thread/session loss and can be recovered logically.

## Decisions
- Treat workspace files as durable memory, not chat threads.
- Add `memory/chat-index.md` as a compact cheat sheet/index of notable conversations and workstreams.
- Add `scripts/session_memory.py` to search, summarize, auto-tag, and index historical session transcripts from `~/.openclaw/agents/main/sessions/`.
- Add `scripts/project_context.sh` / `scripts/project_resume.sh` as quick helpers for resuming projects from prior chat context.
- Add `memory/projects/` for project-specific rollups.
- Update startup/process docs so future work checks project memory, chat index, and historical session logs when a task references prior work or missing context.
- Add a recurring cron reminder every 4 hours to trigger chat-memory rollups in the main session.
- Add a selective LLM-summary path for only fresh/uncataloged sessions, with local indexing remaining the default first pass.

## Files Added / Updated
- `CHAT_MEMORY_SYSTEM.md`
- `memory/chat-index.md`
- `memory/chat-summaries/*.json`
- `memory/chat-summaries-llm/*.json` (path added; generation still being refined)
- `memory/projects/*.md`
- `scripts/session_memory.py`
- `scripts/project_context.sh`
- `scripts/project_resume.sh`
- `scripts/chat_memory_rollup.py`
- `scripts/llm_summarize_session.py`
- `AGENTS.md`
- `HEARTBEAT.md`
- `MEMORY.md`

## Why
JL noticed thread history can be lost. New system is meant to preserve summaries in durable files while keeping full transcript search available on demand.

## Current Limits
- Local summaries/tags are working now.
- Project-memory scaffolding is in place.
- Direct `openclaw agent --local`/main-lane transcript summarization hit session-locking behavior when invoked from inside the active session.
- Isolated subagent summarization was successfully tested and is the correct path for higher-quality structured chat summaries.
- Integration is now in place at the workflow level: isolated LLM summary JSON is being written into `memory/chat-summaries-llm/`, and `memory/projects/` can be rebuilt directly from those files.
- Added pace-based chat rotation policy across OpenClaw chats, with heuristic state in `memory/chat-pace-state.json` and helper script `scripts/chat_pace.py`.
- Added `scripts/chat_pace.py assess` so the system can automatically recommend stay vs codify vs rotate based on elapsed time, pace classification, project switches, and recovery/decision signals.
- Added `scripts/chat_rotate.py` to perform a rotation checkpoint: rebuild indexes/project memory, mark codified, reset pace state, write a rotation note to daily memory, and produce a user-facing summary of saved projects.
- Added `scripts/rotate_notice.py` to generate a human-friendly rotate notice with suggested fresh-chat openers based on recently saved project topics.
- Added `memory/session-registry.json` + `scripts/work_chat.py` as a friendly work-chat manager so JL can think in projects/work chats rather than raw OpenClaw session entries.

## Later updates
- Added pace-aware chat rotation with `scripts/chat_pace.py assess`, heartbeat-triggered rotate/codify flow, and `scripts/chat_rotate.py` checkpoints.
- Added `scripts/rotate_notice.py` to generate human-friendly rotate notices with saved-project highlights and suggested fresh-chat openers.
- Confirmed current OpenClaw Control UI appears to expose session browsing/switching but not an obvious true "new chat" / fresh top-level chat button in this version.
- Added `memory/session-registry.json` + `scripts/work_chat.py` to hide raw session clutter behind a friendlier "current work chat" model.
- Set current work chat to `OpenClaw workspace`, then switched it to `Paper Trader` when JL wanted to resume that area.
- Investigated Paper Trader live state: season-004 is active with 12 bots registered; bot executor is still trying to run all 12 and throwing repeated SQL errors (`change_24h_pct` missing), while JL wants season 4 simplified to just the gainer + loser bots and the extra broken championship dashboards cleaned up.
- Important ambiguity still pending before destructive Paper Trader cleanup: JL wants seasons 2 and 3 to "keep going," but DB currently marks seasons 2 and 3 as paused, so future work should confirm whether to reactivate them in DB/runtime or simply leave their data/configs untouched while fixing season 4.
- JL confirmed the preferred Paper Trader move: keep seasons 2 and 3 trading, delete the extra Season 4 championship dashboards, simplify Season 4 to just `loser_reversal_hunter` and `gainer_momentum_catcher`, and retool the remaining Season 4 dashboard accordingly.
- Executed Paper Trader cleanup: marked seasons 2/3 active, changed Season 4 bot executor to only run the two S4 bots, deleted extra S4 dashboard JSON files, and restarted the relevant containers.
- Then cleaned the remaining Season 4 dashboard and reset Season 4 data/state so it now contains only the two intended bots with a fresh start (new balances/metrics, old Season 4 multi-bot artifacts removed).
- A real rotate checkpoint was performed and a user-facing notice was successfully generated listing saved projects and suggested fresh-chat openers.
- Remaining improvement is ergonomic: make heartbeat-triggered runs automatically spawn isolated summarizers for fresh sessions without manual orchestration from the main assistant turn.

## Rotation checkpoint (2026-03-26 16:50 UTC)
- Demo rotation after automation setup.

## Rotation checkpoint (2026-03-26 17:00 UTC)
- Manual rotate test requested by JL.

## Rotation checkpoint (2026-03-26 23:15 UTC)
- Rotation recommended by chat pace policy.

## Rotation checkpoint (2026-03-27 03:15 UTC)
- Rotation recommended by chat pace policy.
