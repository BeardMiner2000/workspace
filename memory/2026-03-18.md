# 2026-03-18

## Coinbase API credentials (stored in paper-trader-league/.env only, gitignored)
## Tuque Live Trading Plan
- Launch date: Monday March 24 2026
- Capital per bot: 0.015 BTC
- Number of bots: TBD (3-4, decided by Sunday)
- Pain threshold: HIGH — JL comfortable with full loss; BTC he'd hold for years anyway
- No hard stop-loss required; need bug/runaway circuit breakers only
- Guardrails: max 30% per order, API error circuit breaker, daily trade cap, balance floor alert

## CB_API_KEY: organizations/3a330719-b18a-4353-9f97-ab059f9957ce/apiKeys/55b4d61d-82b6-4d49-ab15-13192918e231
- CB_API_SECRET: stored in .env (EC private key, view-only permissions)
- Do NOT commit .env — already gitignored

- Recovered the paper-trader-league stack after an interrupted shutdown: restarted all containers, verified TimescaleDB state, and fixed the root-cause bug where `data_ingest` re-bootstraped (and wiped) seasons on every restart. Added Grafana persistence and committed the fix (d4be186).
- Built `scripts/runtime_healthcheck.py` plus documentation so the experiment can self-verify containers, APIs, DB freshness, and leaderboard data; committed in 961686e.
- Created `scripts/watch_runtime.sh`, added it to the runbook, and started it via `nohup` so it runs every 60 seconds and automatically calls `docker compose up -d` whenever the health check fails. This keeps the 3-day experiment alive without manual babysitting.
